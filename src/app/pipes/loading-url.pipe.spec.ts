import { LoadingUrlPipe } from './loading-url.pipe';

describe('LoadingUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new LoadingUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
