import { CharacterListPipe } from './character-list.pipe';

describe('CharacterListPipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterListPipe();
    expect(pipe).toBeTruthy();
  });
});
