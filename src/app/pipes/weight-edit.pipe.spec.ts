import { WeightEditPipe } from './weight-edit.pipe';

describe('WeightEditPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightEditPipe();
    expect(pipe).toBeTruthy();
  });
});
